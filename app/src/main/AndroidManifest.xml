<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rebuildcareer.jseapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_SMS"
        tools:node="remove"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rebuild_career"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".employe.CompanyDetailsEmployer"/>
        <activity android:name=".employe.EmployeeSignUp"/>
        <activity android:name=".employe.EmployerActivity"/>
        <activity android:name=".employe.MessageEmployer"/>
        <activity android:name=".jobSeeker.JobseekerActivity"/>
        <activity android:name=".jobSeeker.JobSeekerTermsAndConditions"/>
        <activity android:name=".jobSeeker.PhoneAuthActivity"/>
        <activity android:name=".jobSeeker.PhoneNumberActivity"/>
    </application>

</manifest>